[Unit]
Description=McLellan Hub
After=network.target

[Service]
Type=simple
User=hub
WorkingDirectory=/app
EnvironmentFile=/app/.env
ExecStart=/usr/bin/node server.js
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
