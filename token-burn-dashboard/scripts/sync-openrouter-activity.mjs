#!/usr/bin/env node
// OpenRouter activity sync is RETIRED. Historical openrouter-live.* JSON remains
// for the token-burn dashboard as historical/retired data. This script no longer
// contacts OpenRouter management or analytics APIs.
console.log('[token-burn] OpenRouter activity sync retired — historical data retained, no network call.');
process.exit(0);
