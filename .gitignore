node_modules/
.tools/
data/
exports/
.env
.env.*
!.env.example
config/google-service-account.json
mclellan-hub-*.json
config/mclellan-hub-*.json
*.db
*.db-shm
*.db-wal
*.sqlite
__pycache__/
*.pyc
.DS_Store
.claude/
*.zip
*.rtf
*.csv
.rsyncignore
.fuse_hidden*
output/
tmp/

# Generated nightly by com.mclellan.token-burn-refresh (launchd) and rsynced to the VPS directly — not tracked
token-burn-dashboard/deploy-data/daily-burn.sample.json
token-burn-dashboard/deploy-data/openrouter-activity.summary.json
token-burn-dashboard/deploy-data/openrouter-live.summary.json
token-burn-dashboard/deploy-data/openrouter-live.daily.json

# One-off deliverable workspaces (Beacon roadmap etc.) — local only
outputs/
tools/

# Self-repair venue (Mac mini only) — throwaway worktrees and the isolated
# Pi SDK install that must never reach the prod package.json. The queue and
# receipts live under data/ which is already ignored.
.repair-worktrees/
scripts/repair/node_modules/

# Standalone Python scratch in the repo root — kept on the Mac, not part of the
# Node app and never deployed.
/slugify.py
/test_slugify.py

# Change-management dashboard ticket data — not source, not tracked
.hub-changes/
