const users = {
  douglas: {
    hubAdmin: {
      mcpToken: process.env.DCHAT_MCP_TOKEN,
    },
  },
  nakai: {
    hubAdmin: {
      mcpToken: process.env.NCHAT_MCP_TOKEN,
    },
  },
};

module.exports = users;
